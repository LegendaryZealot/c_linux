extern void test();

int add(int a,int b)
{
    test();
    return (a + b);
}

int sub(int a, int b)
{
    test();
    return (a - b);
}

int mul(int a, int b)
{
    test();
    return (a * b);
}

int div(int a, int b)
{
    test();
    return (a / b);
}
